# Model Channel Dashboard 部署文档

## 一键部署（推荐）

### 第一步：运行部署向导

在 PowerShell 中执行：

```powershell
cd "E:\Project\Model Channel Dashboard\model-channel-dashboard"
powershell -ExecutionPolicy Bypass -File "setup.ps1"
```

向导会自动：
1. ✅ 检测 Node.js 和 Python 环境
2. ✅ 扫描本机 VPN / 代理端口（Clash、V2Ray、云鸟等）
3. ✅ 让你选择代理端口和 API 端口
4. ✅ 安装 npm 和 Python 依赖
5. ✅ 保存配置到 `config.json`
6. ✅ 生成 `start.bat` 和 `stop.bat`

### 第二步：启动服务

双击 **`start.bat`** 即可启动全部服务。

### 第三步：停止服务

双击 **`stop.bat`** 即可停止全部服务。

---

## 配置文件说明

部署完成后会生成 `config.json`，内容示例：

```json
{
  "apiPort": 4180,
  "frontendPort": 4173,
  "proxyUrl": "http://127.0.0.1:17890",
  "pythonPath": ".venv\\Scripts\\python.exe"
}
```

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `apiPort` | API 服务端口 | 4180 |
| `frontendPort` | Vite 前端端口 | 4173 |
| `proxyUrl` | VPN 代理地址（空=不使用） | 空 |
| `pythonPath` | Python 可执行文件路径 | 空（自动检测） |

### 如何修改配置

**方式一**：重新运行 `setup.ps1`（交互式向导）

**方式二**：直接编辑 `config.json`

**方式三**：命令行临时指定

```powershell
$env:MODEL_DASHBOARD_API_PORT = "4181"
$env:BANANA_HTTP_PROXY = "http://127.0.0.1:17890"
npm run start
```

---

## 文件清单

```
model-channel-dashboard/
├── setup.ps1            # 一键部署向导（首次运行）
├── config.json          # 端口和环境配置（自动生成）
├── start.bat            # 一键启动（自动生成）
├── stop.bat             # 一键停止（自动生成）
├── vite.config.js       # 自动读取 config.json 的端口配置
├── server.mjs           # API 服务（读取环境变量）
├── package.json         # Node.js 依赖和命令
├── src/                 # React 前端代码
├── public/data/         # 离线展示数据
├── scripts/             # 数据准备脚本
├── relaywatch/          # RelayWatch 爬取脚本
├── .venv/               # Python 虚拟环境
├── node_modules/        # Node.js 依赖
└── README.md            # 项目说明
```

---

## 环境要求

| 组件 | 最低版本 | 用途 |
|------|----------|------|
| Node.js | 18+ | 前端构建 + API 服务 |
| Python | 3.10+ | 数据抓取（可选） |
| VPN 代理 | HTTP 代理 | 访问海外数据源（可选） |

---

## 环境变量参考

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `MODEL_DASHBOARD_API_PORT` | API 端口 | 4180 |
| `VITE_PORT` | 前端端口 | 4173 |
| `BANANA_HTTP_PROXY` | HTTP 代理 | `http://127.0.0.1:10090` |
| `MODEL_DASHBOARD_PYTHON` | Python 路径 | `python` |
| `RELAYWATCH_PYTHON` | RelayWatch Python 路径 | 同上 |

---

## 在新电脑上部署步骤

```powershell
# 1. 克隆项目
git clone https://github.com/shuof794-spec/model-channel-dashboard.git
cd model-channel-dashboard

# 2. 运行部署向导（交互式配置端口和代理）
powershell -ExecutionPolicy Bypass -File "setup.ps1"

# 3. 后续启动只需双击 start.bat
```

---

## 故障排查

### 端口被占用

`start.bat` 会自动跳过被占用的端口。如需手动释放：

```powershell
# 查看占用端口的进程
Get-NetTCPConnection -LocalPort 4180

# 杀掉进程
Stop-Process -Id <PID> -Force
```

### 代理连接失败

1. 确认 VPN 正在运行
2. 运行 `setup.ps1` 重新选择代理端口
3. 或手动编辑 `config.json` 修改 `proxyUrl`

### Python 模块缺失

```powershell
.\.venv\Scripts\python.exe -m pip install -r relaywatch\requirements.txt
```

### 页面打不开

1. 确认 `start.bat` 启动了两个窗口（API + Vite）
2. 检查 `config.json` 中的端口配置
3. 浏览器访问 `http://127.0.0.1:<前端端口>`

---

## 常用命令

```powershell
cd "E:\Project\Model Channel Dashboard\model-channel-dashboard"

# 部署向导
powershell -ExecutionPolicy Bypass -File "setup.ps1"

# 启动/停止
.\start.bat
.\stop.bat

# 手动启动
npm run dev      # 仅前端
npm run start    # 仅 API
npm run build    # 构建生产版本
```
